# TODO - Push app to GitHub

- [ ] Verify git status and whether a git repo already exists in /home/mwas/Desktop/network-simulator
- [ ] Ensure .gitignore correctly excludes node_modules/, dist/, build/, logs, pid files, etc.
- [ ] Create (or update) README.md for clarity (optional but recommended)
- [ ] Create a new empty repository on GitHub named appropriately (e.g., network-simulator)
- [ ] Initialize git if needed, add remote origin, create initial commit on main
- [ ] Push to GitHub: git push -u origin main
- [ ] Validate by checking the repository content on GitHub
- [ ] Run local build checks (npm install + npm run build) to ensure nothing critical was missed

